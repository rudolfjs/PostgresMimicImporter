kind: Fixed
body: Table→CSV file lookup in `_write_mimic_data` now matches by exact basename equality instead of substring. The previous `next(s for s in files if table in s)` silently picked `poe_detail.csv.gz` when looking up table `poe` (and `emar_detail.csv.gz` for `emar`). Duplicates and absences now raise instead of being papered over.
time: 2026-05-22T21:56:00.000000000+10:00
