kind: Added
body: GitHub Actions CI — `pixi run -e dev` matrix for `lint`, `check-format`, `typecheck`, `test`; plus `docker compose config` validation for both the root and devcontainer compose files. Actions pinned to full commit SHAs.
time: 2026-05-22T13:22:11.000000000+10:00
